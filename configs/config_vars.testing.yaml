worker-threads: 4
worker-fs-threads: 2
worker-grpc-threads: 2
logger-level: debug

is-testing: true

server-port: 8080
server-grpc-port: 8081

secdist-path: /secure_data.json

# user_service_db-1 is the service name + _ + filename of the db_1.sql
dbconnection: 'postgresql://testsuite@localhost:15433/user_service_db_1'
